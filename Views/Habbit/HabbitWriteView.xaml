<UserControl x:Class="HabbitFlow.Views.Habbit.HabbitWriteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HabbitFlow.Views.Habbit"
             xmlns:converters="clr-namespace:HabbitFlow.Utilities.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Style="{StaticResource Usercontrol_Dlya_Main}" >

    <UserControl.Resources>
        <converters:HabbitTypeToStringConverter x:Key="HabitTypeToStringConverter"/>
        <converters:HabitTypeToColorConverter x:Key="HabitTypeToColorConverter"/>
    </UserControl.Resources>

    <Grid >
        <ComboBox SelectedValue="{Binding CurrentHabit.Type}" 
                  ItemsSource="{Binding HabitTypes}"
                  Margin="20">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Border Width="16" Height="16" CornerRadius="3" Margin="0,0,8,0"
                                Background="{Binding   Converter={StaticResource HabitTypeToColorConverter}}"/>
                        <TextBlock Text="{Binding   Converter={StaticResource HabitTypeToStringConverter}}"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <Border Background="{Binding CurrentHabit.Type, Converter={StaticResource HabitTypeToColorConverter}}"
                Width="200" Height="100" Margin="20,80,0,0"
                CornerRadius="8">
            <TextBlock Text="{Binding CurrentHabit.Name}" 
                       VerticalAlignment="Center" HorizontalAlignment="Center"
                       FontWeight="SemiBold"/>
        </Border>
    </Grid>
</UserControl>