<UserControl x:Class="HabbitFlow.Views.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#4CAF50" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:PackIcon Kind="Cog" Grid.Column="0" 
                                        Foreground="White" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="Настройки" 
                          FontSize="22" FontWeight="Bold" 
                          Foreground="White" VerticalAlignment="Center" Margin="15,0,0,0"/>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Background="#F8F9FA">
            <StackPanel Margin="20" MaxWidth="500">
                <!-- Application Settings -->
                <Border Background="White" CornerRadius="8" BorderThickness="1" BorderBrush="#E0E0E0" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Настройки приложения" 
                                  FontSize="16" FontWeight="Bold" Foreground="#333333"
                                  Margin="0,0,0,20"/>

                        <!-- Language -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="Язык:" 
                                      FontSize="13" FontWeight="Medium" Foreground="#333333"
                                      VerticalAlignment="Center" Width="120"/>
                            <ComboBox ItemsSource="{Binding Languages}"
                                     SelectedItem="{Binding SelectedLanguage}"
                                     BorderThickness="1" BorderBrush="#E0E0E0" 
                                     Padding="10" Background="White"
                                     Width="200"/>
                        </StackPanel>

                        <!-- Theme -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="Тема:" 
                                      FontSize="13" FontWeight="Medium" Foreground="#333333"
                                      VerticalAlignment="Center" Width="120"/>
                            <ComboBox ItemsSource="{Binding Themes}"
                                     SelectedItem="{Binding SelectedTheme}"
                                     BorderThickness="1" BorderBrush="#E0E0E0" 
                                     Padding="10" Background="White"
                                     Width="200"/>
                        </StackPanel>

                        <!-- Notifications -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="Уведомления:" 
                                      FontSize="13" FontWeight="Medium" Foreground="#333333"
                                      VerticalAlignment="Center" Width="120"/>
                            <CheckBox IsChecked="{Binding EnableNotifications}"
                                     Content="Включить уведомления"
                                     Foreground="#333333"
                                     FontSize="13"/>
                        </StackPanel>

                        <Button Content="Сохранить настройки" 
                                Background="#4CAF50"
                                Foreground="White"
                                BorderThickness="0"
                                
                               
                                FontWeight="SemiBold"
                                Command="{Binding SaveSettingsCommand}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
