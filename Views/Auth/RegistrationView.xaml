<Window x:Class="HabbitFlow.Views.Auth.RegistrationView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local ="clr-namespace:HabbitFlow.Views.USERCONTROL"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        WindowStyle="None" AllowsTransparency="True"
        Background="White" WindowStartupLocation="CenterScreen"
         Width =" 900"
		 Height="400" >

    <Grid Margin="0,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="Регистрация" Style="{StaticResource MaterialDesignHeadline2TextBlock}" Grid.ColumnSpan="2" TextAlignment="Center" Width="400" Margin="250,0,250,44"/>
        <Rectangle  />
        <Button
            x:Name="Nazad"
            Content="⬅️" 
            Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
            Background="Green"       
            Foreground="White"
            
            Height="34"
            Width="35"
            FontSize="20"
            Opacity="0"
            FontWeight="SemiBold"
            Padding="0"
            
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Command="{Binding NavigateToLoginCommand}" 
            Margin="10,10,0,0"
            
            ToolTip="Назад">

            

            <Button.Effect>
                <DropShadowEffect ShadowDepth="2" Color="Green" Opacity="0.5" BlurRadius="3"/>
            </Button.Effect>

           
            <Button.Triggers>
                <EventTrigger RoutedEvent="Button.MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" 
                                             To="1.1" Duration="0:0:0.1"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" 
                                             To="1.1" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="Button.MouseLeave">
                    
                        <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" 
                                             To="1" Duration="0:0:0.1"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" 
                                             To="1" Duration="0:0:0.1"/>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.3"/>
                        </Storyboard>
                    </BeginStoryboard>
                    
                </EventTrigger>
               
            </Button.Triggers>

            
            <Button.RenderTransform>
                <ScaleTransform/>
            </Button.RenderTransform>

        </Button>

    </Grid>


</Window>
