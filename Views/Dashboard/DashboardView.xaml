<UserControl x:Class="HabbitFlow.Views.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:colorm="clr-namespace:MaterialDesignColors;assembly=MaterialDesignColors"
             Width="1630" Height="1006">







    <Border CornerRadius="20" Background="White">

        <ScrollViewer VerticalScrollBarVisibility="Auto" Background="Transparent">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>


                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>

                </Grid.RowDefinitions>


                <Label Grid.Row="0" Grid.Column="0" Content="Habits Today" 
                   FontSize="52" FontFamily="Segoe UI Semibold"  />
                <Button  Grid.Row="0" Grid.Column="2" Background="Aqua" Width="420" Height="70" HorizontalAlignment="Right" 
                         Style="{StaticResource  ButtonSignUp}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button" >
                            <Border CornerRadius="12" BorderThickness="1" BorderBrush="Black"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Margin="0 20 0 20" CanHorizontallyScroll="True" Orientation="Horizontal">
                    <Button Margin="0 0 0 0" Width="500" Height="80" >
                        <Button.Background>
                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                <GradientStop Color="#FFB16459"/>
                                <GradientStop Color="#FFE0DA3F" Offset="1"/>
                                <GradientStop Color="#FED8C929" Offset="0.015"/>
                            </LinearGradientBrush>
                        </Button.Background>
                    </Button>
                    <Button Margin="20 0 0 0" Width="500" Height="80" Background="Black"/>
                    <Button Margin="20 0 0 0" Width="500" Height="80" Background="Black"/>

                </StackPanel>

            </Grid>
            
        </ScrollViewer>
    </Border>
</UserControl>