<Window x:Class="HabbitFlow.Views.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        WindowStyle="None" AllowsTransparency="True"
        Background="White" WindowStartupLocation="CenterScreen"
        Height="400" Width="426">

    <Window.Clip>
        <RectangleGeometry Rect="0 0 426 400"   RadiusX="25" RadiusY="25"/>

    </Window.Clip>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        
        <TextBlock  Style="{StaticResource MaterialDesignBody2TextBlock}" Margin="120,203,0,6" HorizontalAlignment="Left"
                   Text="HabbitFlow"
           FontFamily="/proekt;component/Resources/Nunito-Regular.ttf#Nunito"
           FontSize="39" FontWeight="Bold" Foreground="#7CB342"/>
        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                 IsIndeterminate="True" Width="74" Foreground="LightGreen" 
                 HorizontalAlignment="Center" Margin="0,215,0,-15" Grid.RowSpan="2"/>
        <TextBlock Text="Mrlexman" Opacity="0.5" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0 0 340 0" Grid.RowSpan="2"/>
    </Grid>
</Window>
